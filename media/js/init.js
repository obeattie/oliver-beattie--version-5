// Global page initialization stuff

// Font definitions
var didot_ital = {
    src: '/media/flash/sifr/didot-ital.swf'
}

// Font activation
sIFR.activate(didot_ital)

// Binding to elements
sIFR.replace(didot_ital, {
    selector: '.sifr.didot'
});
